# 参与贡献

感谢你愿意改进微存。

## 开发流程

1. Fork 仓库并从 `main` 创建分支。
2. 运行 `npm install`。
3. 使用 `npm run dev` 启动静态网站。
4. 在 Chromium 扩展管理页加载仓库中的 `extension/`。
5. 提交前运行：

```bash
npm run check
npm test
npm run build
```

## 微博解析兼容

修复页面或 API 解析时，请添加一个最小、脱敏的测试样例。不要提交：

- Cookie、Token 或账号资料
- 未经许可的大段文章原文
- 私密文章内容
- 完整微博页面快照

优先在 `test/parser.test.js` 中构造只包含相关字段的 HTML 或 JSON。

## 扩展安全边界

- 不增加通配的任意网络请求能力。
- 网页只能传文章 ID 和固定接口编号，不能把完整请求 URL 交给扩展。
- 新的网站来源必须同时加入 Manifest 注入范围和后台来源校验。
- 不记录或回传 Token、Cookie。
- 新权限必须在 Pull Request 中解释必要性。

## 设计约定

- 保持内容优先的白底编辑风格，不引入后台仪表盘式卡片矩阵。
- 普通用户不应理解跨域、IndexedDB 等术语才能完成保存。
- 新交互需要覆盖键盘焦点、等待、成功、错误和移动端状态。
- 不依赖颜色作为唯一的状态表达方式。
